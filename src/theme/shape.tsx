// Define an interface for the shape object
interface Shape {
    borderRadius: number;
    borderRadiusSm: number;
    borderRadiusMd: number;
  }
  
  // Define the shape object
  const shape: Shape = {
    borderRadius: 8,
    borderRadiusSm: 12,
    borderRadiusMd: 16
  };
  
  export default shape;
  